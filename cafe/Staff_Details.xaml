<Page x:Class="cafe.Staff_Details"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:cafe"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Staff_Details">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="NameTextBox" Width="200" Margin="0,0,10,0" />
            <!--<Button x:Name="AddButton" Content="Add Staff" Click="AddButton_Click"/>-->
        </StackPanel>

        <ListView Grid.Row="1" x:Name="StaffListView" Margin="10">
            <ListView.View>
                <GridView>
                    <!--<GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Code" DisplayMemberBinding="{Binding Code}"/>-->
                </GridView>
            </ListView.View>
        </ListView>
    </Grid> 
</Page>
